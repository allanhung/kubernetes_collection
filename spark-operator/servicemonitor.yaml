apiVersion: v1
kind: Service
metadata:
  labels:
    service: spark-application-metrics
  name: spark-application-metrics
  namespace: spark
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: jmx
    port: 8090
    protocol: TCP
    targetPort: 8090
  selector:
    sparkoperator.k8s.io/launched-by-spark-operator: "true"
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: po
  name: spark-application-metrics
  namespace: infra
spec:
  selector:
    matchLabels:
      service: spark-application-metrics
  namespaceSelector:
    matchNames:
    - spark
  endpoints:
  - interval: 5s
    port: jmx
    path: /metrics
